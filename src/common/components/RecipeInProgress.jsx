import React, { } from 'react';
// import store from '../../context/store';

export default function RecipeInProgress() {
  // const { recipes: { recipeDetail } } = useContext(store);

  return (
    <h1>Estamos na página de progresso</h1>
  );
}
