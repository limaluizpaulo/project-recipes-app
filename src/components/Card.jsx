import React from 'react';

export default function Card(/* props */) {
  // const { data } = props;
  return (
    <div>
      <img src="blabla" alt="thumbnail" />
      <span>{ }</span>
    </div>
  );
}
