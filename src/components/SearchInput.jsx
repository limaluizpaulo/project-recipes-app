import React from 'react';

function SearchInput() {
  // console.log(display);
  return (
    <div
      // style={ `display: ${display}` }
      data-testid="search-input"
    >
      Aqui vai ser a procura
    </div>
  );
}

export default SearchInput;
