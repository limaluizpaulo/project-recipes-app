// import React from 'react';
// import Card from './Card';

// const CardsGrid = ({ array }) => array.map(({ strMeal, strMealThumb, idMeal }) => (
//   <Card key={ idMeal } title={ strMeal } img={ strMealThumb } />
// ));

// export default CardsGrid;
