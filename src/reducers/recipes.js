const INITIAL_STATE = {
  inProgressRecipes: [],
  recipesDefault: [],
  filteredRecipes: [],
};

function recipes(state = INITIAL_STATE, { type }) {
  switch (type) {
  default:
    return state;
  }
}

export default recipes;
